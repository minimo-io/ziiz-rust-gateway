#!/usr/bin/env bash

# SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

# docker compose --env-file "$SCRIPT_DIR/.env" up -d

# without composer
# docker build -t ziiz .
# docker run -p 5000:8080 865ff0969fd7